
import { renderString } from '../../src/index';
describe(`Evaluates expression without printing out result.`, () => {
  it(`unnamed case 0`, () => {
      const html = renderString(`{% do list.append('value 2') %}`);
      
  });
});