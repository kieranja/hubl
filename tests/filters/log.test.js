
import { renderString } from '../../src/index';
describe(`Return the natural log of the input.`, () => {
  it(`unnamed case 0`, () => {
      const html = renderString(`{{ 25|log(5) }}`);
      
  });
});