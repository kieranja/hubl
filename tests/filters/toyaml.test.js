
import { renderString } from '../../src/index';
describe(`Writes object as a YAML string`, () => {
  it(`unnamed case 0`, () => {
      const html = renderString(`{{object|toyaml}}`);
      
  });
});