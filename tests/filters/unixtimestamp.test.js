
import { renderString } from '../../src/index';
describe(`Gets the UNIX timestamp value (in milliseconds) of a date object`, () => {
  it(`unnamed case 0`, () => {
      const html = renderString(`{% mydatetime|unixtimestamp %}`);
      
  });
});