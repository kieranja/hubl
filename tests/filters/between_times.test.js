
import { renderString } from '../../src/index';
describe(`Calculates the time between two datetime objects`, () => {
  it(`unnamed case 0`, () => {
      const html = renderString(`{% begin|between_times(end, 'hours') %}`);
      
  });
});