
import { renderString } from '../../src/index';
describe(`Return the square root of the input.`, () => {
  it(`unnamed case 0`, () => {
      const html = renderString(`{{ 125|root(3) }}`);
      
  });
});