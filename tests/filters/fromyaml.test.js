
import { renderString } from '../../src/index';
describe(`Converts a YAML string to an object`, () => {
  it(`unnamed case 0`, () => {
      const html = renderString(`{{object|fromYaml}}`);
      
  });
});