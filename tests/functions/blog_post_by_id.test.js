
import { renderString } from '../../src/index';
describe(`Returns a blog post by id`, () => {
  it(`unnamed case 0`, () => {
      const html = renderString(`{{ blog_post_by_id(123) }}`);
      
  });
});