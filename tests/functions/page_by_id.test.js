
import { renderString } from '../../src/index';
describe(`Returns a landing or website page by id`, () => {
  it(`unnamed case 0`, () => {
      const html = renderString(`{{ page_by_id(123) }}`);
      
  });
});