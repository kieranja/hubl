
import { renderString } from '../../src/index';
describe(`Returns the value of a contact or contact related property, or a default.`, () => {
  it(`unnamed case 0`, () => {
      const html = renderString(`{{ personalization_token("contact.firstname", "there") }}`);
      
  });
});