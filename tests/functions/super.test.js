
import { renderString } from '../../src/index';
describe(`Only usable within blocks, will render the contents of the parent block by calling super.`, () => {
  it(`This gives back the results of the parent block`, () => {
//       const html = renderString(`{% block sidebar %}
//     <h3>Table Of Contents</h3>

//     ...
//     {{ super() }}
// {% endblock %}`);
      
  });
});