
import { renderString } from '../../src/index';
describe(`Returns an m.me link for facebook messenger`, () => {
  it(`unnamed case 0`, () => {
      const html = renderString(`{{ facebook_messenger_link() }}`);
      
  });
});