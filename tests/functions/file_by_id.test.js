
import { renderString } from '../../src/index';
describe(`Returns the metadata of a file by ID`, () => {
  it(`unnamed case 0`, () => {
      const html = renderString(`{{ file_by_id(123) }}`);
      
  });
});