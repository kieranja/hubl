
import { renderString } from '../../src/index';
describe(`Returns URL to specified template by id`, () => {
  it(`unnamed case 0`, () => {
      const html = renderString(`{{ get_public_template_url_by_id('2778457004') }}`);
      
  });
});