
import { renderString } from '../../src/index';
describe(`Gets the URL for an asset attached to a module`, () => {
  it(`unnamed case 0`, () => {
      const html = renderString(`{{ module_asset_url("smile.jpg") }}`);
      
  });
});