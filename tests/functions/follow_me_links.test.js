
import { renderString } from '../../src/index';
describe(`Returns a list of configured follow me links for the portal`, () => {
  it(`unnamed case 0`, () => {
      const html = renderString(`{{ follow_me_links() }}`);
      
  });
});